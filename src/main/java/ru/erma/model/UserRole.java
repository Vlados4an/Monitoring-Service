package ru.erma.model;

/**
 * This enum represents the different roles a user can have in the system.
 * A user can be a guest, a regular user, or an admin.
 */
public enum UserRole {
    GUEST,
    USER,
    ADMIN
}
