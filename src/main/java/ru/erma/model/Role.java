package ru.erma.model;

/**
 * The Role enum represents the roles a user can have in the system.
 * Each instance of this enum corresponds to a single role.
 * Currently, there are two roles: USER and ADMIN.
 */
public enum Role {
    USER,
    ADMIN
}